<?xml version="1.0" encoding="ISO-8859-1" ?>
<pipe:pipeline xmlns:pipe="http://cancerresearchuk.org/pipeline/1.2"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://cancerresearchuk.org/pipeline/1.2 http://uk-cri-lsol03:8080/solexa/solexa01/schema/workflow/pipeline-1.2.xsd">

    <name>ChipBamProcess Pipeline</name>
    <description>Pipeline for generating Bam files processed for the ChIPseqPipeline.</description>

    <tasks>
        


	<specialisationSet name="MergeCaller" type="MergeCaller" variable="trio">

	<subtasks> 
       
	 <task name="MergeProcess" type="MergeProcess">
        <resources>
            <memory>8G</memory>
        </resources>
            <outputs>
                <output name="MergedFile" deleteOnFailure="false">${WorkingDirectory}/Temp/${OutName}_TMP.bam</output>
            </outputs>
    	<arguments>
    	    <arg name="InputVector">${InputName}</arg>
    	    <arg name="TheDirectory">${WorkingDirectory}</arg>
	    <arg name="OutputName">${OutName}</arg>
    	</arguments> 
            
	</task>


	<task name="MarkDuplicateProcess" type="MarkDuplicateProcess">
       <depends>
		<task>MergeProcess</task>
	</depends>
	<resources>
            <memory>8G</memory>
        </resources>
	  <inputs>
		 <input name="MergedFile">${WorkingDirectory}/Temp/${OutName}_TMP.bam</input>
	 </inputs>
            <outputs>
                <output name="MarkedFile" deleteOnFailure="false">${WorkingDirectory}/bamFiles/${OutName}.bwa.bam</output>
            </outputs>
    	<arguments>
    	    <arg name="TheDirectory">${WorkingDirectory}</arg>
	    <arg name="OutputName">${OutName}</arg>
    	</arguments> 
	</task>

	     </subtasks>
      </specialisationSet>
    </tasks>
</pipe:pipeline>